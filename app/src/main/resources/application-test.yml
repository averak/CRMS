spring:
  datasource:
    url: jdbc:mysql://localhost:3307/crms?useSSL=false&allowPublicKeyRetrieval=true
    username: crms
    password: crms
    driver: com.mysql.cj.jdbc.Driver

slack:
  enabled: false
  webhook-url:

logging:
  level:
    root: ERROR
